# 유적지 Sokoban 정식 퍼즐 설계 사양

이 문서의 Stage 2 2-1~2-3은 워밍업으로 유지하고, 2-4~2-10은 재설계했다. 이전 목업 보드는 [목업 기록](ruins-sokoban-prototype-stage2.json)에 보존했다. [설계 JSON](ruins-sokoban-puzzles.json)의 Stage 2 보드와 힌트는 현재 게임의 `src/config/ruinsSokoban/stage2.ts`에 반영했다. Stage 1의 7문제는 유지하며 Stage 3는 게임에 연결하지 않았다.

## 검증 방법과 해석

[solver](../scripts/ruins-sokoban-design-solver.mjs)는 플레이어·상자 위치의 유한 상태를 완전 탐색한다. **최소 밀기**는 밀기 수를 먼저 최적화한 값이고, **최소 이동**은 이동 수를 독립적으로 먼저 최적화한 값이다. 둘이 다른 경우를 위해 *최소 밀기 해의 이동 수*도 표에 적었다. `U/D/L/R`은 위/아래/왼쪽/오른쪽이다. [검증 스크립트](../scripts/ruins-sokoban-verify-design.mjs)는 모든 보드의 해법, 비교 기준 목업과의 불일치, 패턴 회피 탐색을 확인하고 [재현 가능한 결과 JSON](ruins-sokoban-solver-report.json)을 만든다. [런타임 일치 테스트](../src/config/ruinsSokoban/stage2.test.ts)는 Stage 2의 모든 보드·힌트·파싱 결과와 실제 이동 규칙으로 재생한 해법을 확인한다.

- **거리 증가 밀기:** 모든 상자와 목표의 최소 맨해튼 배정 거리 합이 증가하는 밀기다. 이 밀기를 모두 금지한 탐색이 실패하면 '목표에서 일시적으로 멀어지기'가 필수다. 실제 벽을 고려한 목표 배정 거리와는 다른 보수적인 측정 기준이다.
- **목표에서 상자 빼기:** 목표 위 상자를 비목표 칸으로 다시 미는 수다. 이를 금지한 탐색이 실패하면 임시 배치·통로 확보가 필수다.
- **상자 순서·목표 배정:** 시작 시 위에서 아래, 왼쪽에서 오른쪽 순으로 번호를 붙인 상자를 추적한다. 지정한 선행 상자보다 다른 상자를 먼저 밀게 하거나, 지정 목표 배정을 금지하고 다시 완전 탐색한다.
- **대체 해법:** 아래 표의 숫자는 *정답으로 이어지는 다른 첫 밀기*의 개수다. 1 이상이면 별도 풀이 경로가 실제로 있다. 0은 첫 밀기 대안이 없다는 뜻이며, 이후 경로나 단순 걷기 순서까지 유일하다는 뜻은 아니다. '쉬운 회피'는 의도한 패턴을 금지한 별도 탐색으로 검사했다.
- **교착:** 비목표 정적 모서리와 첫 밀기 직후 더 이상 풀 수 없는 위치를 적는다. 뒤늦게 생기는 동적 교착 전체를 열거한 것은 아니다. Undo는 언제든 사용할 수 있게 설계한다.

좌표는 왼쪽 위를 (1,1)로 하는 (열,행)이다. 보드는 `#` 벽, `@` 플레이어, `$` 상자, `.` 목표, `*` 목표 위 상자, `+` 목표 위 플레이어, `E` 장식 출구다. 클리어 판정은 모든 상자의 목표 배치다.

## 15문제 요약

| 미션 | 크기 | 상자/목표 | 최소 밀기 | 최소 이동 | 최소 밀기 해 이동 | 대체 첫 밀기 | 핵심 패턴의 필수성 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | --- |
| 2-1 기둥을 돌아 자리 잡기 | 8×7 | 2/2 | 6 | 16 | 18 | 2 | 왼쪽 밀기 금지 시 불가능; 위쪽 밀기 금지 시 불가능 |
| 2-2 먼저 비워야 할 길 | 8×7 | 2/2 | 8 | 31 | 31 | 0 | 아래 상자를 위 상자보다 늦게 움직이면 불가능 |
| 2-3 좁은 통로의 차례 | 8×7 | 3/3 | 8 | 23 | 23 | 3 | 왼쪽 상자를 가운데 상자보다 늦게 움직이면 불가능 |
| 2-4 멀어졌다 가까워져요 | 7×7 | 2/2 | 6 | 19 | 19 | 2 | 거리 증가 밀기 금지 시 불가능 |
| 2-5 비워야 지나가요 | 8×7 | 3/3 | 10 | 30 | 30 | 2 | 목표에서 상자 빼기 금지 시 불가능 |
| 2-6 서로 막는 두 상자 | 8×7 | 2/2 | 8 | 25 | 25 | 0 | 왼쪽 상자를 오른쪽 상자보다 늦게 움직이면 불가능 |
| 2-7 어느 상자가 어느 제단으로? | 8×7 | 3/3 | 11 | 25 | 25 | 2 | 첫 상자의 왼쪽 목표 배정 금지 시 불가능; 거리 증가 밀기 금지 시 불가능 |
| 2-8 제단에서 잠깐 내려요 | 8×7 | 3/3 | 9 | 22 | 22 | 3 | 목표에서 상자 빼기 금지 시 불가능; 거리 증가 밀기 금지 시 불가능 |
| 2-9 막힌 길을 다시 열어요 | 9×7 | 3/3 | 11 | 22 | 22 | 3 | 거리 증가 밀기 금지 시 불가능; 목표에서 상자 빼기 금지 시 불가능 |
| 2-10 유적 훈련 최종 시험 | 9×7 | 4/4 | 12 | 28 | 28 | 6 | 거리 증가 밀기 금지 시 불가능; 목표에서 상자 빼기 금지 시 불가능 |
| 3-1 고대 석판의 방 | 9×7 | 3/3 | 11 | 32 | 32 | 5 | 거리 증가 밀기 금지 시 불가능; 목표에서 상자 빼기 금지 시 불가능 |
| 3-2 공룡 문장의 방 | 8×7 | 3/3 | 10 | 28 | 28 | 5 | 거리 증가 밀기 금지 시 불가능 |
| 3-3 에메랄드의 방 | 8×7 | 3/3 | 11 | 22 | 22 | 2 | 첫 상자의 왼쪽 목표 배정 금지 시 불가능; 아래 상자를 오른쪽 위 상자보다 늦게 움직이면 불가능 |
| 3-4 룬 핵의 방 | 9×7 | 4/4 | 11 | 22 | 22 | 5 | 거리 증가 밀기 금지 시 불가능; 목표에서 상자 빼기 금지 시 불가능 |
| 3-5 태양 장치의 방 | 9×7 | 4/4 | 13 | 27 | 27 | 5 | 거리 증가 밀기 금지 시 불가능; 왼쪽 상자를 바로 옆 상자보다 늦게 움직이면 불가능 |

목업 기준점은 2-1 **5/16**, 2-2 **6/37**, 2-3 **8/19**였다. 2-1~2-3은 이번 재조정에서 보드를 유지했다. 숫자만으로 아동 체감 난이도의 순서를 확정하지 않는다. 특히 2-5(30이동)는 아이 테스트에서 피로도를 확인해야 한다.

2-2는 첫 밀기 대안이 없고, 최적해의 각 밀기 사건을 하나씩 금지해도 해가 사라졌다. 따라서 확인한 보드에서는 다른 밀기 사건을 이용하는 대안이 없다. 단순 걷기 경로의 차이는 대체 해법으로 세지 않는다.

## Stage 2 재조정 검증

| 미션 | 제단 이탈 필수 | 거리 증가 필수 | 필수 임시 칸 | 풀리는 첫 밀기 / 즉시 해법 소멸 | 가장 빠른 잘못된 첫 선택 |
| --- | --- | --- | --- | --- | --- |
| 2-1 | 아니요 | 아니요 | 없음 | 3/1 | 1 push |
| 2-2 | 예 | 예 | 없음 | 1/3 | 1 push |
| 2-3 | 예 | 예 | 없음 | 4/2 | 1 push |
| 2-4 | 아니요 | 예 | 없음 | 3/1 | 1 push |
| 2-5 | 예 | 예 | (5,4) | 3/3 | 1 push |
| 2-6 | 아니요 | 예 | 없음 | 1/1 | 1 push |
| 2-7 | 아니요 | 예 | (6,4) | 3/2 | 1 push |
| 2-8 | 예 | 예 | (5,5) | 4/4 | 1 push |
| 2-9 | 예 | 예 | (6,5)·(7,4) | 4/4 | 1 push |
| 2-10 | 예 | 예 | (6,3)·(7,4) | 7/5 | 1 push |

**'즉시 해법 소멸'은 solver 판정 시점**이다. 사람이 막힘을 알아차리는 시점과 다르다. 2~4 push 뒤에 아이가 막힘을 깨닫는지는 자동 탐색으로 판정하지 못했다. 2-10에서는 오른쪽 위 상자를 목표 (7,2)에 바로 올리는 첫 밀기가 성공처럼 보이지만 그 직후 해법이 없다. 보고서의 `trappedFirstPushes`는 각 선택의 좌표와 방향을 재현한다.

**필수 임시 칸**은 최단해에서 한 상자를 비목표 칸에 놓고 다른 상자를 민 뒤 원래 상자를 다시 움직이는 칸이다. 그 칸으로의 모든 밀기를 금지해 해가 사라질 때만 표에 적었다. 제단 이탈 필수 검사는 시작부터 제단 위에 있던 상자의 이동도 포함한다. 플레이 중 새로 올린 상자를 다시 빼는지 여부는 `newlyPlacedGoalExitsInMinPushSolution`에 별도 기록한다.

사람 관점에서 2-4는 아래 상자를 먼저 움직여 밀기 면을 만들고, 2-5는 제단 상자를 비운 뒤 통로를 열고, 2-6은 두 상자의 이동 순서를 고른다. 2-7은 왼쪽 목표 배정과 임시 퇴보, 2-8은 제단 회수와 통로 우회, 2-9는 제단 회수와 아래 상자 이동을 결합한다. 2-10은 ① 오른쪽 위 상자 후퇴, ② 가운데 제단 비우기, ③ 왼쪽 상자 이동, ④ 목표 배정 마무리의 네 판단이 연결된다.

## 미션별 보드와 해법

각 미션의 '첫 밀기 함정'은 처음 접근 가능한 밀기 중 그 한 수 이후 해가 없는 목적지다. 정적 모서리와 겹칠 수 있다. 힌트 3은 해당 최적해에서 실제로 가능한 첫 밀기 한 번만 알려주며 전체 경로를 공개하지 않는다.

### 2-1 기둥을 돌아 자리 잡기

~~~text
########
# . .$ #
#      #
#  ##$ #
#     @#
#     E#
########
~~~

- **ID·크기·시작:** `ruins-sokoban-2-1`, 8×7, (7,5).
- **상자·목표:** (6,2)·(6,4) → (3,2)·(5,2). 벽 전체는 위 보드의 `#`다.
- **핵심/필수 사고:** 기본 우회와 밀기 면 확보. 왼쪽 밀기 금지 시 불가능; 위쪽 밀기 금지 시 불가능. 이 제한 탐색에서는 패턴 회피 해법이 없다.
- **Solver:** 해결 가능, 최소 밀기 6, 독립 최소 이동 16, 최소 밀기 해의 이동 18. 주요 해법 `UUULLLDLDDRRRUURUL`(밀기만 `LLLUUL`).
- **대체 해법:** 다른 첫 밀기 2개가 정답으로 이어진다. 이후 다른 밀기 순서는 별도 유일성 주장 대상이 아니다.
- **교착 주의:** 비목표 정적 모서리 (2,2)·(7,2)·(2,6)·(7,6). 첫 밀기 함정 목적지 (7,2).
- **단계 힌트:** ① 밀 자리를 찾아봐요. ② 위 상자 오른쪽을 봐요. ③ 위 상자를 왼쪽으로 한 칸 밀어봐요.

### 2-2 먼저 비워야 할 길

~~~text
########
# $. . #
#   #  #
# #  # #
# @  $ #
#    #E#
########
~~~

- **ID·크기·시작:** `ruins-sokoban-2-2`, 8×7, (3,5).
- **상자·목표:** (3,2)·(6,5) → (4,2)·(6,2). 벽 전체는 위 보드의 `#`다.
- **핵심/필수 사고:** 상자 처리 순서. 아래 상자를 위 상자보다 늦게 움직이면 불가능. 이 제한 탐색에서는 패턴 회피 해법이 없다.
- **Solver:** 해결 가능, 최소 밀기 8, 독립 최소 이동 31, 최소 밀기 해의 이동 31. 주요 해법 `RUUURRDRDDLLULULLURRRLDDRDDLUUU`(밀기만 `LLRRRUUU`).
- **대체 해법:** 다른 첫 밀기 0개는 확인되지 않았다. 이후 다른 밀기 순서는 별도 유일성 주장 대상이 아니다.
- **교착 주의:** 비목표 정적 모서리 (2,2)·(7,2)·(6,3)·(5,4)·(2,6)·(5,6)·(7,6). 첫 밀기 함정 목적지 (2,2)·(4,2)·(7,5).
- **단계 힌트:** ① 순서를 생각해요. ② 아래 오른쪽 상자를 봐요. ③ 그 상자를 왼쪽으로 한 칸 밀어봐요.

### 2-3 좁은 통로의 차례

~~~text
########
# . .  #
#  # . #
# $$ $ #
#   #  #
# @   E#
########
~~~

- **ID·크기·시작:** `ruins-sokoban-2-3`, 8×7, (3,6).
- **상자·목표:** (3,4)·(4,4)·(6,4) → (3,2)·(5,2)·(6,3). 벽 전체는 위 보드의 `#`다.
- **핵심/필수 사고:** 통로 진입 순서. 왼쪽 상자를 가운데 상자보다 늦게 움직이면 불가능. 이 제한 탐색에서는 패턴 회피 해법이 없다.
- **Solver:** 해결 가능, 최소 밀기 8, 독립 최소 이동 23, 최소 밀기 해의 이동 23. 주요 해법 `UUULURRLDDDDRRRUULLDLUU`(밀기만 `UURRULUU`).
- **대체 해법:** 다른 첫 밀기 3개가 정답으로 이어진다. 이후 다른 밀기 순서는 별도 유일성 주장 대상이 아니다.
- **교착 주의:** 비목표 정적 모서리 (2,2)·(7,2)·(2,6)·(7,6). 첫 밀기 함정 목적지 (5,4)·(7,4).
- **단계 힌트:** ① 통로를 막지 마세요. ② 왼쪽 상자를 봐요. ③ 왼쪽 상자를 위로 한 칸 밀어봐요.

### 2-4 멀어졌다 가까워져요

~~~text
#######
#     #
#  . .#
# @$  #
#  #$ #
#    E#
#######
~~~

- **ID·크기·시작:** `ruins-sokoban-2-4`, 7×7, (3,4).
- **상자·목표:** (4,4)·(5,5) → (4,3)·(6,3). 벽 전체는 위 보드의 `#`다.
- **핵심/필수 사고:** 목표에서 멀어지는 밀기와 오른쪽 밀기 자리 확보. 거리 증가 밀기 금지 시 불가능. 이 제한 탐색에서는 패턴 회피 해법이 없다.
- **Solver:** 해결 가능, 최소 밀기 6, 독립 최소 이동 19, 최소 밀기 해의 이동 19. 주요 해법 `DDRRUURULULLDDRRDRU`(밀기만 `UULRRU`).
- **대체 해법:** 다른 첫 밀기 2개가 정답으로 이어진다. 이후 다른 밀기 순서는 별도 유일성 주장 대상이 아니다.
- **교착 주의:** 비목표 정적 모서리 (2,2)·(6,2)·(2,6)·(6,6). 첫 밀기 함정 목적지 (5,6).
- **단계 힌트:** ① 바로 목표로 밀기 전에, 다음에 설 자리를 찾아봐요. ② 아래 상자를 옮기면 어느 쪽에 설 수 있을까요? ③ 아래 상자를 위로 한 칸 밀어봐요.

### 2-5 비워야 지나가요

~~~text
########
# .  * #
# #$#  #
#      #
#  . $ #
# @  #E#
########
~~~

- **ID·크기·시작:** `ruins-sokoban-2-5`, 8×7, (3,6).
- **상자·목표:** (6,2)·(4,3)·(6,5) → (3,2)·(6,2)·(4,5). 벽 전체는 위 보드의 `#`다.
- **핵심/필수 사고:** 목표 위 상자를 비우고 아래 통로의 밀기 면 확보. 목표에서 상자 빼기 금지 시 불가능. 이 제한 탐색에서는 패턴 회피 해법이 없다.
- **Solver:** 해결 가능, 최소 밀기 10, 독립 최소 이동 30, 최소 밀기 해의 이동 30. 주요 해법 `URURRRDLULLDDRURUURULLLDDRDRUU`(밀기만 `LULLLDDRUU`).
- **대체 해법:** 다른 첫 밀기 2개가 정답으로 이어진다. 이후 다른 밀기 순서는 별도 유일성 주장 대상이 아니다.
- **교착 주의:** 비목표 정적 모서리 (2,2)·(7,2)·(2,6)·(5,6)·(7,6). 첫 밀기 함정 목적지 (7,2)·(4,2)·(7,5).
- **단계 힌트:** ① 제단에 놓인 상자도 아직 움직일 수 있어요. ② 아래 통로에 서려면 무엇을 먼저 비워야 할까요? ③ 오른쪽 아래 상자를 왼쪽으로 한 칸 밀어봐요.

### 2-6 서로 막는 두 상자

~~~text
########
# ..   #
#  #   #
# $  # #
# $#  ##
#  @  E#
########
~~~

- **ID·크기·시작:** `ruins-sokoban-2-6`, 8×7, (4,6).
- **상자·목표:** (3,4)·(3,5) → (3,2)·(4,2). 벽 전체는 위 보드의 `#`다.
- **핵심/필수 사고:** 상자 간 간섭과 목표 반대 방향의 임시 이동. 왼쪽 상자를 오른쪽 상자보다 늦게 움직이면 불가능. 이 제한 탐색에서는 패턴 회피 해법이 없다.
- **Solver:** 해결 가능, 최소 밀기 8, 독립 최소 이동 25, 최소 밀기 해의 이동 25. 주요 해법 `LLUURRLLDDRUUUDDDRRUUURUL`(밀기만 `RRUUUUUL`).
- **대체 해법:** 다른 첫 밀기 0개는 확인되지 않았다. 이후 다른 밀기 순서는 별도 유일성 주장 대상이 아니다.
- **교착 주의:** 비목표 정적 모서리 (2,2)·(7,2)·(7,4)·(6,5)·(2,6)·(7,6). 첫 밀기 함정 목적지 (2,4).
- **단계 힌트:** ① 두 상자 중 하나가 다른 상자의 길을 만들어요. ② 위쪽 왼편 상자를 먼저 살펴봐요. ③ 그 상자를 오른쪽으로 한 칸 밀어봐요.

### 2-7 어느 상자가 어느 제단으로?

~~~text
########
#.# . .#
# # #  #
#$# $  #
#@  $  #
# #   E#
########
~~~

- **ID·크기·시작:** `ruins-sokoban-2-7`, 8×7, (2,5).
- **상자·목표:** (2,4)·(5,4)·(5,5) → (2,2)·(5,2)·(7,2). 벽 전체는 위 보드의 `#`다.
- **핵심/필수 사고:** 목표 배정과 통로를 위한 임시 퇴보. 첫 상자의 왼쪽 목표 배정 금지 시 불가능; 거리 증가 밀기 금지 시 불가능. 이 제한 탐색에서는 패턴 회피 해법이 없다.
- **Solver:** 해결 가능, 최소 밀기 11, 독립 최소 이동 25, 최소 밀기 해의 이동 25. 주요 해법 `UUDDRRURLDRRUURULDDDDRUUU`(밀기만 `UURRRUULUUU`).
- **대체 해법:** 다른 첫 밀기 2개가 정답으로 이어진다. 이후 다른 밀기 순서는 별도 유일성 주장 대상이 아니다.
- **교착 주의:** 비목표 정적 모서리 (4,2)·(2,6)·(4,6)·(7,6). 첫 밀기 함정 목적지 (4,4)·(4,5).
- **단계 힌트:** ① 왼쪽 제단에는 어느 상자가 갈 수 있을까요? ② 가운데 통로를 먼저 차지하면 오른쪽 상자는 어떻게 될까요? ③ 왼쪽 상자를 위로 한 칸 밀어봐요.

### 2-8 제단에서 잠깐 내려요

~~~text
########
# .  * #
#  #$  #
#  #   #
#    *@#
#     E#
########
~~~

- **ID·크기·시작:** `ruins-sokoban-2-8`, 8×7, (7,5).
- **상자·목표:** (6,2)·(5,3)·(6,5) → (3,2)·(6,2)·(6,5). 벽 전체는 위 보드의 `#`다.
- **핵심/필수 사고:** 제단의 상자 회수와 통로 우회. 목표에서 상자 빼기 금지 시 불가능; 거리 증가 밀기 금지 시 불가능. 이 제한 탐색에서는 패턴 회피 해법이 없다.
- **Solver:** 해결 가능, 최소 밀기 9, 독립 최소 이동 22, 최소 밀기 해의 이동 22. 주요 해법 `DLUURUULLLRDDRUDDDLLUR`(밀기만 `UULLLDDUR`).
- **대체 해법:** 다른 첫 밀기 3개가 정답으로 이어진다. 이후 다른 밀기 순서는 별도 유일성 주장 대상이 아니다.
- **교착 주의:** 비목표 정적 모서리 (2,2)·(7,2)·(2,6)·(7,6). 첫 밀기 함정 목적지 (7,2)·(5,2)·(6,6)·(7,5).
- **단계 힌트:** ① 제단 위 상자를 다시 비워야 할 수도 있어요. ② 아래쪽 제단과 가운데 상자의 길을 함께 봐요. ③ 아래쪽 제단의 상자를 위로 한 칸 밀어봐요.

### 2-9 막힌 길을 다시 열어요

~~~text
#########
# .   * #
# ###$@ #
#   #   #
#  .  $ #
#      E#
#########
~~~

- **ID·크기·시작:** `ruins-sokoban-2-9`, 9×7, (7,3).
- **상자·목표:** (7,2)·(6,3)·(7,5) → (3,2)·(7,2)·(4,5). 벽 전체는 위 보드의 `#`다.
- **핵심/필수 사고:** 목표 회수와 목표 반대 방향 밀기의 결합. 거리 증가 밀기 금지 시 불가능; 목표에서 상자 빼기 금지 시 불가능. 이 제한 탐색에서는 패턴 회피 해법이 없다.
- **Solver:** 해결 가능, 최소 밀기 11, 독립 최소 이동 22, 최소 밀기 해의 이동 22. 주요 해법 `RULLLLRRDDRRDDLULLRRUU`(밀기만 `LLLLDDULLUU`).
- **대체 해법:** 다른 첫 밀기 3개가 정답으로 이어진다. 이후 다른 밀기 순서는 별도 유일성 주장 대상이 아니다.
- **교착 주의:** 비목표 정적 모서리 (2,2)·(8,2)·(4,4)·(2,6)·(8,6). 첫 밀기 함정 목적지 (8,2)·(6,2)·(7,6)·(8,5).
- **단계 힌트:** ① 윗길은 상자를 놓으면 막힐 수 있어요. ② 위쪽 제단과 오른쪽 상자를 함께 봐요. ③ 제단 위 상자를 왼쪽으로 한 칸 밀어봐요.

### 2-10 유적 훈련 최종 시험

~~~text
#########
# . * .##
#   # $ #
# $  @  #
# #  .$ #
#      E#
#########
~~~

- **ID·크기·시작:** `ruins-sokoban-2-10`, 9×7, (6,4).
- **상자·목표:** (5,2)·(7,3)·(3,4)·(7,5) → (3,2)·(5,2)·(7,2)·(6,5). 벽 전체는 위 보드의 `#`다.
- **핵심/필수 사고:** 임시 퇴보·제단 회수·상자 재배치가 이어지는 최종 순서. 거리 증가 밀기 금지 시 불가능; 목표에서 상자 빼기 금지 시 불가능. 이 제한 탐색에서는 패턴 회피 해법이 없다.
- **Solver:** 해결 가능, 최소 밀기 12, 독립 최소 이동 28, 최소 밀기 해의 이동 28. 주요 해법 `RRULULLLDLLDRRRRURULDRRDDLUU`(밀기만 `LLLRRRRULLUU`).
- **대체 해법:** 다른 첫 밀기 6개가 정답으로 이어진다. 이후 다른 밀기 순서는 별도 유일성 주장 대상이 아니다.
- **교착 주의:** 비목표 정적 모서리 (2,2)·(8,3)·(2,6)·(8,6). 첫 밀기 함정 목적지 (7,2)·(8,3)·(2,4)·(7,6)·(8,5).
- **단계 힌트:** ① 먼저 길을 만들 상자를 찾아봐요. ② 가운데 제단 위 상자는 계속 그곳에 둘 수 있을까요? ③ 오른쪽 위 상자를 왼쪽으로 한 칸 밀어봐요.

### 3-1 고대 석판의 방

~~~text
#########
# . .   #
# ##  $ #
#  ##   #
# $.  $ #
#  @   E#
#########
~~~

- **ID·크기·시작:** `ruins-sokoban-3-1`, 9×7, (4,6).
- **상자·목표:** (7,3)·(3,5)·(7,5) → (3,2)·(5,2)·(4,5). 벽 전체는 위 보드의 `#`다.
- **핵심/필수 사고:** 통로 순서와 우회. 거리 증가 밀기 금지 시 불가능; 목표에서 상자 빼기 금지 시 불가능. 이 제한 탐색에서는 패턴 회피 해법이 없다.
- **Solver:** 해결 가능, 최소 밀기 11, 독립 최소 이동 32, 최소 밀기 해의 이동 32. 주요 해법 `LLURDRRURURURULLLLRRDDDDRUUURULL`(밀기만 `RULLLLUUULL`).
- **대체 해법:** 다른 첫 밀기 5개가 정답으로 이어진다. 이후 다른 밀기 순서는 별도 유일성 주장 대상이 아니다.
- **교착 주의:** 비목표 정적 모서리 (2,2)·(8,2)·(5,3)·(3,4)·(2,6)·(8,6). 첫 밀기 함정 목적지 (8,3)·(3,4)·(3,6)·(2,5)·(7,6)·(8,5).
- **단계 힌트:** ① 길을 연 뒤 제단을 채워요. ② 아래 왼쪽 상자를 봐요. ③ 그 상자를 오른쪽으로 한 칸 밀어봐요.
- **최초 클리어 확정 부품:** `ancient_tablet`. 재클리어 지급 없음.

### 3-2 공룡 문장의 방

~~~text
########
# +.   #
# # #$ #
#      #
# $. $ #
#     E#
########
~~~

- **ID·크기·시작:** `ruins-sokoban-3-2`, 8×7, (3,2).
- **상자·목표:** (6,3)·(3,5)·(6,5) → (3,2)·(4,2)·(4,5). 벽 전체는 위 보드의 `#`다.
- **핵심/필수 사고:** 반대 방향 이동과 자리 만들기. 거리 증가 밀기 금지 시 불가능. 이 제한 탐색에서는 패턴 회피 해법이 없다.
- **Solver:** 해결 가능, 최소 밀기 10, 독립 최소 이동 28, 최소 밀기 해의 이동 28. 주요 해법 `RDDRRURULLLDDLLDRDRRRUUURULL`(밀기만 `ULLLRUUULL`).
- **대체 해법:** 다른 첫 밀기 5개가 정답으로 이어진다. 이후 다른 밀기 순서는 별도 유일성 주장 대상이 아니다.
- **교착 주의:** 비목표 정적 모서리 (2,2)·(7,2)·(2,6)·(7,6). 첫 밀기 함정 목적지 (3,6)·(2,5)·(6,6)·(7,5).
- **단계 힌트:** ① 처음에는 멀어져도 돼요. ② 오른쪽 위 상자를 봐요. ③ 그 상자를 위로 한 칸 밀어봐요.
- **최초 클리어 확정 부품:** `dinosaur_skull_emblem`. 재클리어 지급 없음.

### 3-3 에메랄드의 방

~~~text
########
#.# .  #
# ###  #
#$# $  #
#@ $   #
#.    E#
########
~~~

- **ID·크기·시작:** `ruins-sokoban-3-3`, 8×7, (2,5).
- **상자·목표:** (2,4)·(5,4)·(4,5) → (2,2)·(5,2)·(2,6). 벽 전체는 위 보드의 `#`다.
- **핵심/필수 사고:** 목표 배정과 상자 간섭. 첫 상자의 왼쪽 목표 배정 금지 시 불가능; 아래 상자를 오른쪽 위 상자보다 늦게 움직이면 불가능. 이 제한 탐색에서는 패턴 회피 해법이 없다.
- **Solver:** 해결 가능, 최소 밀기 11, 독립 최소 이동 22, 최소 밀기 해의 이동 22. 주요 해법 `UUDDRRURDRUURULDDDDLLL`(밀기만 `UURRDUULLLL`).
- **대체 해법:** 다른 첫 밀기 2개가 정답으로 이어진다. 이후 다른 밀기 순서는 별도 유일성 주장 대상이 아니다.
- **교착 주의:** 비목표 정적 모서리 (4,2)·(7,2)·(4,4)·(7,6). 첫 밀기 함정 목적지 (4,4)·(4,4).
- **단계 힌트:** ① 왼쪽 제단의 상자를 정해요. ② 좁은 왼쪽 방을 봐요. ③ 왼쪽 상자를 위로 한 칸 밀어봐요.
- **최초 클리어 확정 부품:** `emerald_stone`. 재클리어 지급 없음.

### 3-4 룬 핵의 방

~~~text
#########
# * . * #
#   #  .#
#@$     #
# #   $ #
#      E#
#########
~~~

- **ID·크기·시작:** `ruins-sokoban-3-4`, 9×7, (2,4).
- **상자·목표:** (3,2)·(7,2)·(3,4)·(7,5) → (3,2)·(5,2)·(7,2)·(8,3). 벽 전체는 위 보드의 `#`다.
- **핵심/필수 사고:** 임시 배치와 통로 확보. 거리 증가 밀기 금지 시 불가능; 목표에서 상자 빼기 금지 시 불가능. 이 제한 탐색에서는 패턴 회피 해법이 없다.
- **Solver:** 해결 가능, 최소 밀기 11, 독립 최소 이동 22, 최소 밀기 해의 이동 22. 주요 해법 `RRRRURRULLDDDRDRUUDLUU`(밀기만 `RRRRLLRUUUU`).
- **대체 해법:** 다른 첫 밀기 5개가 정답으로 이어진다. 이후 다른 밀기 순서는 별도 유일성 주장 대상이 아니다.
- **교착 주의:** 비목표 정적 모서리 (2,2)·(8,2)·(2,6)·(8,6). 첫 밀기 함정 목적지 (2,2)·(8,2)·(2,4)·(7,6).
- **단계 힌트:** ① 제단을 잠깐 비워봐요. ② 아래 왼쪽 상자를 봐요. ③ 그 상자를 오른쪽으로 한 칸 밀어봐요.
- **최초 클리어 확정 부품:** `rune_core`. 재클리어 지급 없음.

### 3-5 태양 장치의 방

~~~text
#########
#   . . #
#. #    #
# $$$$  #
#  #  . #
# @    E#
#########
~~~

- **ID·크기·시작:** `ruins-sokoban-3-5`, 9×7, (3,6).
- **상자·목표:** (3,4)·(4,4)·(5,4)·(6,4) → (5,2)·(7,2)·(2,3)·(7,5). 벽 전체는 위 보드의 `#`다.
- **핵심/필수 사고:** 네 상자 종합과 교착 회피. 거리 증가 밀기 금지 시 불가능; 왼쪽 상자를 바로 옆 상자보다 늦게 움직이면 불가능. 이 제한 탐색에서는 패턴 회피 해법이 없다.
- **Solver:** 해결 가능, 최소 밀기 13, 독립 최소 이동 27, 최소 밀기 해의 이동 27. 주요 해법 `UUULURRRRDDRDDLLURLUUDLLDLU`(밀기만 `UURRRRDRUULLU`).
- **대체 해법:** 다른 첫 밀기 5개가 정답으로 이어진다. 이후 다른 밀기 순서는 별도 유일성 주장 대상이 아니다.
- **교착 주의:** 비목표 정적 모서리 (2,2)·(8,2)·(2,6)·(8,6). 첫 밀기 함정 목적지 없음.
- **단계 힌트:** ① 제단 순서를 생각해요. ② 왼쪽 상자를 봐요. ③ 왼쪽 상자를 위로 한 칸 밀어봐요.
- **최초 클리어 확정 부품:** `sun_gear`. 재클리어 지급 없음.

## 난이도 곡선과 남은 검토

2-1~2-3은 워밍업으로 유지했다. 2-4~2-6은 한 번의 Undo와 순서 판단, 2-7~2-9는 두 패턴의 결합, 2-10은 네 단계의 연쇄 판단을 목표로 한다. 보드 크기는 기존 7×7~9×7 범위를 유지했다. 2-5는 사고 요소와 함께 이동 수도 늘었으므로 아이가 걷기 피로를 느끼는지 우선 관찰해야 한다.

solver의 회피 탐색은 정의된 기계적 패턴의 필수성을 증명한다. 첫 선택이 2~4 push 뒤에야 잘못되었음을 **사람이 알아차리는지**는 증명하지 못한다. 실제 아이 테스트에서 시도한 첫 밀기, 막힘을 깨달은 push 번호, Undo 횟수, 힌트 사용량을 기록하고 늦은 좌절이나 즉시 보이는 모서리 함정이 많으면 후속 조정한다. Stage 3의 보드와 구현은 이번 재조정 범위에서 변경하지 않았다.
